% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_substitution_elbow}
\alias{plot_substitution_elbow}
\title{Plot the delta_r distribution as a ranked elbow curve}
\usage{
plot_substitution_elbow(substitutions, delta_threshold = NULL, n_label = 5)
}
\arguments{
\item{substitutions}{A \code{substitutions} object
(\code{\link{detect_substitutions}}).}

\item{delta_threshold}{Threshold line to draw. NULL (default) reads it from
\code{attr(substitutions, "params")$delta_threshold} if present, else 0.3.}

\item{n_label}{Label the top \code{n_label} genes by delta_r. Default 5;
0 suppresses labels.}
}
\value{
A ggplot object: gene rank (x) vs delta_r (y), threshold line marked.
}
\description{
Ranks every scored gene by delta_r = r_paralog - r_ortholog (descending) and
plots the sorted curve, with the flagging threshold drawn as a reference
line. If delta_r has a natural break between substitution candidates and the
smooth bulk, that bend is the candidate set, read off visually; if it is
smooth, the plot shows that honestly and marks where the default lands.
}

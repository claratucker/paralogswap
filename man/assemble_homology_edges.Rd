% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homology.R
\name{assemble_homology_edges}
\alias{assemble_homology_edges}
\title{Assemble homology edges from raw ortholog/paralog rows (internal)}
\usage{
assemble_homology_edges(
  ortholog_edges,
  paralog_edges,
  species_a,
  species_b,
  corrections = NULL,
  min_perc_id = NULL,
  min_confidence = "low",
  verbose = TRUE
)
}
\arguments{
\item{ortholog_edges}{data.frame of cross-species ortholog edges (both
directions stacked), with the standard edge columns.}

\item{paralog_edges}{data.frame of within-species paralog edges (both
species stacked), or NULL.}

\item{species_a, species_b}{Focal and target species prefixes.}

\item{corrections}{Optional user edge table.}

\item{min_perc_id, min_confidence}{Filters (see build_homology_graph).}

\item{verbose}{Passed to as_homology_graph.}

\item{mirror}{Optional Ensembl mirror: one of "useast", "uswest", "asia".
Can be faster/more reliable than the default www.ensembl.org for large pulls.}
}
\value{
A homology_graph.
}
\description{
Pure logic: takes the two directional ortholog pulls and the per-species
paralog pulls (each already normalized to the edge-table columns), unions and
dedupes them, normalizes ortholog direction to the focal species, applies
filters, and merges corrections. No network. Separated from
\code{build_homology_graph} so the assembly logic is unit-testable.
}
\keyword{internal}
